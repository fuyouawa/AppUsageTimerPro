<UserControl x:Class="AppUsageTimerPro.View.Tools.DisplayTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AppUsageTimerPro.View.Tools"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding Display}"
                   Width="{Binding DisplayWidth}"
                   Height="{Binding DisplayHeight}"
                   Margin="{Binding DisplayMargin}"
                   FontSize="{Binding DisplayFontSize}"/>
        <TextBox mah:TextBoxHelper.ClearTextButton="True"
                 mah:TextBoxHelper.Watermark="{Binding PlaceHolder}"
                 Width="{Binding TextWidth}"
                 Height="{Binding TextHeight}"
                 FontSize="{Binding TextFontSize}"
                 Text="{Binding Text}"
                 Margin="{Binding TextMargin}"/>
    </StackPanel>
</UserControl>
